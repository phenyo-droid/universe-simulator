

class Planet {

    constructor(radius, angle, distance){
        this.angle = 0;
        this.radius = r;
        this.distance = d
    }

    draw(){

        ellipse(0, 0, radius*2, radius*2)
    }
}